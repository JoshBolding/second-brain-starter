# 99 Sensitive Review

Quarantine folder for notes containing credentials, personal data, or anything else agents should never read or summarize. The skill instructs agents to skip this folder entirely, and the vault `.gitignore` keeps it out of version control.

Move a note here first, sanitize it, then file it — never the other way around.
